name: Release application

on:
  push:

permissions:
  contents: read
  id-token: write

env:
  PROJECT_ID: ${{ secrets.GKE_PROJECT }}
  GKE_CLUSTER: dwk-cluster
  GKE_ZONE: europe-north1-b
  REGISTRY: europe-north1-docker.pkg.dev
  REPOSITORY: devops-with-kubernetes
  FRONTEND_IMAGE: devops-with-kubernetes-frontend
  BACKEND_IMAGE: devops-with-kubernetes-backend
  STATEFULSET: devops-with-kubernetes-project-ss
  NAMESPACE: the-project
  BRANCH: ${{ github.ref_name }}

jobs:
  build-publish-deploy:
    name: Build, Publish and Deploy
    runs-on: ubuntu-latest
    environment: GKE_PROJECT

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: "${{ secrets.WORKLOAD_IDENTITY_PROVIDER }}"
          service_account: "${{ secrets.SERVICE_ACCOUNT }}"

      - name: 'Set up Cloud SDK'
        uses: google-github-actions/setup-gcloud@v3

      - name: 'Use gcloud CLI'
        run: gcloud info

      - name: 'Configure Docker authentication'
        run: gcloud --quiet auth configure-docker "$REGISTRY"

      - name: 'Get GKE credentials'
        uses: 'google-github-actions/get-gke-credentials@v3'
        with:
          cluster_name: '${{ env.GKE_CLUSTER }}'
          project_id: '${{ env.PROJECT_ID }}'
          location: '${{ env.GKE_ZONE }}'

      - name: 'Form the image names'
        run: |-
          echo "FRONTEND_IMAGE_TAG=$REGISTRY/$PROJECT_ID/$REPOSITORY/$FRONTEND_IMAGE:$BRANCH-$GITHUB_SHA" >> "$GITHUB_ENV"
          echo "BACKEND_IMAGE_TAG=$REGISTRY/$PROJECT_ID/$REPOSITORY/$BACKEND_IMAGE:$BRANCH-$GITHUB_SHA" >> "$GITHUB_ENV"

      - name: Build
        run: |-
          docker build --tag "$FRONTEND_IMAGE_TAG" TheProject/frontend
          docker build --tag "$BACKEND_IMAGE_TAG" TheProject/backend

      - name: Publish
        run: |-
          docker push "$FRONTEND_IMAGE_TAG"
          docker push "$BACKEND_IMAGE_TAG"

      - name: Generate Kubernetes Secret
        env:
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
        run: |-
          : "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD secret is not configured}"
          kubectl create secret generic project-postgres-password \
            --namespace "$NAMESPACE" \
            --from-literal=POSTGRES_PASSWORD="$POSTGRES_PASSWORD" \
            --dry-run=client \
            --output yaml > TheProject/manifests/secret.yaml

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v3

      - name: Deploy
        working-directory: TheProject/manifests
        run: |-
          kustomize edit set image \
            eetujukka/devops-with-kubernetes-frontend="$FRONTEND_IMAGE_TAG" \
            eetujukka/devops-with-kubernetes-backend="$BACKEND_IMAGE_TAG"
          kustomize build . | kubectl apply -f -
          kubectl rollout status statefulset/"$STATEFULSET" --namespace "$NAMESPACE"
          kubectl get services --namespace "$NAMESPACE" -o wide