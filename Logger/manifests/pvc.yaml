apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: log-ping-pong-claim
  namespace: logger
spec:
  storageClassName: standard-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
