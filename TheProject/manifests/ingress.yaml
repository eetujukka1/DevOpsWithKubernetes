apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-with-kubernetes-project-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devops-with-kubernetes-project-svc
            port:
              number: 2345