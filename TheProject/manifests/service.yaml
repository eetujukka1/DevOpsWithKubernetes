apiVersion: v1
kind: Service
metadata:
  name: devops-with-kubernetes-project-svc
spec:
  type: NodePort
  selector:
    app: devops-with-kubernetes-project
  ports:
    - name: http
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 3001